<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <%- include("partials/template.ejs") %>
</head>
<body>
<style>
/* Title */
    .title {
      position: absolute;
      left: 500px;
      font-family: 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
      font-weight: 600;
      color: #004080; /* Deep healthcare blue */
      font-size: clamp(24px, 4vw, 36px);
      margin: 40px auto 40px;
      line-height: 1.4; 
      word-break: break-word;
      max-width: 90%;
      text-align: center;
     
    }

/* Main Content */
    .container {
      margin-top: 29vh; 
      max-width: 1200px;
     margin-right: 12%;
      padding: 0 70px;
    }

   .content {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 30px;
  margin-top: -100px; /* ✅ Moves content up by 10px */
}

    /* Cards */
    .card {
      background-color: white;
      border-radius: 12px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      width: calc(33% - 20px);
      padding: 25px;
      text-align: center;
      cursor: pointer;
      transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
    }

    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
    }

    .card h3 {
      font-size: 24px;
      color: #00796b; /* Teal */
      margin-bottom: 15px;
      font-weight: 500;
    }

    .card p {
      color: #666;
      font-size: 16px;
      margin-bottom: 20px;
    }

    .card a {
      color: #00796b; /* Teal */
      font-weight: 500;
      text-decoration: none;
      border: 2px solid #00796b; /* Teal */
      padding: 8px 20px;
      border-radius: 6px;
      transition: background-color 0.3s ease-in-out;
    }

    .card a:hover {
      background-color: #00796b; /* Teal */
      color: white;
    }





</style>
<h1 class="title"><%= __('insurance.heading') %></h1>

<div class="container">
  <div class="content">
    <!-- Claim History Card -->
    <div class="card" onclick="window.location.href='claimList'">
      <h3><%= __('insurance.cards.claimHistory.title') %></h3>
      <p><%= __('insurance.cards.claimHistory.text') %></p>
      <a href="claimList"><%= __('insurance.cards.claimHistory.link') %></a>
    </div>

    <!-- Submit Claim Card -->
    <div class="card" onclick="window.location.href='insuranceClaim'">
      <h3><%= __('insurance.cards.submitClaim.title') %></h3>
      <p><%= __('insurance.cards.submitClaim.text') %></p>
      <a href="insuranceClaim"><%= __('insurance.cards.submitClaim.link') %></a>
    </div>

    <!-- Insurance List Card -->
    <div class="card" onclick="window.location.href='insuranceSearch'">
      <h3><%= __('insurance.cards.insuranceList.title') %></h3>
      <p><%= __('insurance.cards.insuranceList.text') %></p>
      <a href="insuranceSearch"><%= __('insurance.cards.insuranceList.link') %></a>
    </div>

    <!-- Coverage Info Card -->
    <div class="card" onclick="window.location.href='coverageInfo'">
      <h3><%= __('insurance.cards.coverage.title') %></h3>
      <p><%= __('insurance.cards.coverage.text') %></p>
      <a href="coverageInfo"><%= __('insurance.cards.coverage.link') %></a>
    </div>
  </div>
</div>

</body>
</html>