<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= __('insuranceClaimUpdate.title') %></title>
  <%- include("partials/template.ejs") %>
  <style>
    :root {
      --primary: #007bff;
      --primary-dark: #0056b3;
      --card-bg: #fff;
      --text: #333;
      --radius: 12px;
    }

    body {
      margin: 5px;
      font-family: Arial, sans-serif;
      background: #f5f7fa;
      display: flex;
      justify-content: flex-start;
      align-items: flex-start;
      padding: 20px;
      color: var(--text);
    }

    .claim-container {
      position: absolute;
      top: 2px;
      left: 370px;
      background: var(--card-bg);
      border-radius: var(--radius);
      width: 100%;
      max-width: 700px;
      padding: 30px 25px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.08);
      margin-left: 0;
    }

    h2 {
      margin-bottom: 20px;
      font-size: 22px;
      font-weight: bold;
      text-align: center;
    }

    form .form-group {
      margin-bottom: 18px;
      text-align: left;
    }

    label {
      display: block;
      margin-bottom: 6px;
      font-weight: 600;
      color: var(--text);
      font-size: 14px;
    }

    input[type="text"],
    input[type="email"],
    input[type="tel"],
    textarea,
    select {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 14px;
      box-sizing: border-box;
    }

    textarea {
      resize: vertical;
      min-height: 80px;
    }

    .button-row {
      display: flex;
      justify-content: space-between;
      gap: 10px;
      margin-top: 25px;
    }

    .btn {
      flex: 1;
      padding: 12px;
      font-size: 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .btn-save {
      background-color: var(--primary);
      color: white;
    }

    .btn-save:hover { background-color: var(--primary-dark); }

    .btn-cancel {
      background-color: #ddd;
      color: #333;
    }

    .btn-cancel:hover { background-color: #bbb; }

    @media (max-width: 768px) {
      .claim-container {
        padding: 20px 16px;
      }
      h2 { font-size: 20px; }
    }

    @media (max-width: 500px) {
      .button-row { flex-direction: column; }
      .btn { width: 100%; }
    }
  </style>
</head>
<body>
  <div class="claim-container">
    <h2><%= __('insuranceClaimUpdate.header') %></h2>

    <form id="claimForm" action="/updateClaim" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="name"><%= __('insuranceClaimUpdate.fullName') %></label>
        <input type="text" id="name" name="fname" value="<%= claim.fullName %>" required />
      </div>

      <div class="form-group">
        <label for="claimID"><%= __('insuranceClaimUpdate.claimReference') %></label>
        <input type="text" id="claimID" name="claimID" value="<%= claim.claimID %>" required />
      </div>

      <div class="form-group">
        <label for="claimTotal"><%= __('insuranceClaimUpdate.claimTotal') %></label>
        <input type="text" id="claimTotal" name="claimTotal" value="<%= claim.claimTotal %>" required />
      </div>

      <div class="form-group">
        <label for="status"><%= __('insuranceClaimUpdate.status') %></label>
        <input type="text" id="status" name="status" value="<%= claim.status %>" />
      </div>

      <div class="form-group">
        <label for="submittedAt"><%= __('insuranceClaimUpdate.submittedOn') %></label>
        <input type="text" id="submittedAt" name="submittedAt" value="<%= claim.submittedAt %>" />
      </div>

      <div class="form-group">
        <label for="typeOfPolice"><%= __('insuranceClaimUpdate.claimType') %></label>
        <select id="typeOfPolice" name="typeOfPolice" required>
          <option value=""><%= __('insuranceClaimUpdate.selectType') %></option>
          <option value="Dental" <%= claim.typeOfPolice === 'Dental' ? 'selected' : '' %>><%= __('insuranceClaimUpdate.types.dental') %></option>
          <option value="Optometrista" <%= claim.typeOfPolice === 'Optometrista' ? 'selected' : '' %>><%= __('insuranceClaimUpdate.types.optometrista') %></option>
          <option value="Consulta Normal" <%= claim.typeOfPolice === 'Consulta Normal' ? 'selected' : '' %>><%= __('insuranceClaimUpdate.types.regularAppointment') %></option>
          <option value="Medicamentos" <%= claim.typeOfPolice === 'Medicamentos' ? 'selected' : '' %>><%= __('insuranceClaimUpdate.types.medicin') %></option>
        </select>
      </div>

      <div class="form-group">
        <label for="description"><%= __('insuranceClaimUpdate.description') %></label>
        <textarea id="description" name="description" rows="5"><%= claim.description %></textarea>
      </div>

      <div class="form-group">
        <label for="documents"><%= __('insuranceClaimUpdate.uploadDocuments') %></label>
        <input type="file" name="document" />
      </div>

      <div class="button-row">
        <button type="submit" class="btn btn-save"><%= __('insuranceClaimUpdate.update') %></button>
      <button type="button" class="btn btn-cancel" onclick="window.history.back()">
  <%= __('insuranceClaimUpdate.cancel') %>
</button>

      </div>
    </form>

    <a href="/insurance" style="display:block; margin-top:15px;"><%= __('insuranceClaimUpdate.backToHome') %></a>
  </div>
</body>
</html>
