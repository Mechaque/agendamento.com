<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= __('laboratory.title') %></title>
  <%- include("partials/template.ejs") %>

  <style>
    /* Title */
    .title {
      position: absolute;
      left: 360px;
      font-family: 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
      font-weight: 600;
      color: #004080;
      font-size: clamp(24px, 4vw, 36px);
      margin: 40px auto 40px;
      line-height: 1.4;
      word-break: break-word;
      max-width: 90%;
      text-align: center;
    }

    /* Main container */
    .container {
      margin-top: 29vh;
      max-width: 1200px;
      margin-right: 12%;
      padding: 0 70px;
    }

    /* Content cards layout */
    .content {
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      gap: 30px;
      margin-top: -10px; /* ✅ pushes content up */
    }

    /* Cards */
    .card {
      background-color: white;
      border-radius: 12px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      width: calc(33% - 20px);
      padding: 25px;
      text-align: center;
      cursor: pointer;
      transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
    }

    .card:hover {
      transform: translateY(-10px);
      box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
    }

    .card h3 {
      font-size: 24px;
      color: #00796b;
      margin-bottom: 15px;
      font-weight: 500;
    }

    .card p {
      color: #666;
      font-size: 16px;
      margin-bottom: 20px;
    }

    .card a {
      color: #00796b;
      font-weight: 500;
      text-decoration: none;
      border: 2px solid #00796b;
      padding: 8px 20px;
      border-radius: 6px;
      transition: background-color 0.3s ease-in-out;
    }

    .card a:hover {
      background-color: #00796b;
      color: white;
    }
  </style>
</head>

<body>
  <h1 class="title"><%= __('laboratory.header') %></h1>

  <div class="container">
    <div class="content">

      <!-- Lab History -->
      <div class="card" onclick="window.location.href='labHistory'">
        <h3><%= __('laboratory.history.title') %></h3>
        <p><%= __('laboratory.history.desc') %></p>
        <a href="labHistory"><%= __('laboratory.history.link') %></a>
      </div>

      <!-- Search Lab -->
      <div class="card" onclick="window.location.href='searchLab'">
        <h3><%= __('laboratory.search.title') %></h3>
        <p><%= __('laboratory.search.desc') %></p>
        <a href="searchLab"><%= __('laboratory.search.link') %></a>
      </div>

      <!-- Book a Lab Test -->
      <div class="card" onclick="window.location.href='labTestRequest'">
        <h3><%= __('laboratory.book.title') %></h3>
        <p><%= __('laboratory.book.desc') %></p>
        <a href="labTestRequest"><%= __('laboratory.book.link') %></a>
      </div>

    </div>
  </div>
</body>
</html>
