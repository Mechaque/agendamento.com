<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <%- include('partials/header') %>

  <title><%= __('login.title') %></title>

  <style>
    * { box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f4f6f9;
      margin: 0;
      padding: 0;
    }
    .container {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
    }
    .login-box {
      background: #ffffff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 400px;
    }
    .login-box h2 {
      margin-bottom: 25px;
      color: #333;
      text-align: center;
      font-size: 1.8rem;
    }
    .form-group { margin-bottom: 18px; }
    .form-group label {
      display: block;
      margin-bottom: 6px;
      color: #555;
      font-size: 0.95rem;
    }
    .form-group input {
      width: 100%;
      padding: 12px;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 1rem;
    }
    .form-group input:focus { border-color: #007BFF; outline: none; }
    .form-check {
      display: flex;
      align-items: center;
      margin-bottom: 18px;
      font-size: 0.9rem;
    }
    .form-check input { margin-right: 10px; }
    .btn {
      background-color: #007BFF;
      color: white;
      padding: 12px;
      border: none;
      width: 100%;
      font-size: 1rem;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    .btn:hover { background-color: #0056b3; }
    .footer {
      text-align: center;
      margin-top: 18px;
      font-size: 0.9rem;
    }
    .footer a {
      color: #007BFF;
      text-decoration: none;
    }
    .footer a:hover { text-decoration: underline; }
    .lang {
      text-align: right;
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="login-box">

      <h2><%= __('login.heading') %></h2>

      <% if (success && success.length > 0) { %>
        <div class="alert alert-success">
          <%= success %>
        </div>
      <% } %>

      <% if (error && error.length > 0) { %>
        <div class="alert alert-danger">
          <%= error %>
        </div>
      <% } %>

      <form action="/login" method="POST">
        <div class="form-group">
          <label for="email"><%= __('login.email') %></label>
          <input type="email" id="email" name="username" required />
        </div>

        <div class="form-group">
          <label for="password"><%= __('login.password') %></label>
          <input type="password" id="password" name="password" required />
        </div>

        <div class="form-check">
          <input type="checkbox" id="remember" name="remember" />
          <label for="remember"><%= __('login.remember') %></label>
        </div>

        <button type="submit" class="btn"><%= __('login.submit') %></button>
      </form>

      <div class="footer">
        <p><%= __('login.noAccount') %> <a href="/registration"><%= __('login.signUp') %></a></p>
        <p><a href="/forgot-password"><%= __('login.forgotPassword') %></a></p>
      </div>
    </div>
  </div>
</body>
</html>
